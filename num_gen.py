import random
# import numpy as nmpi
import re
import sys

start = input("where we should start > ")
rng = input("Range > ")
len = input("How Much > ")

values = random.sample(range(start, rng), len)
# hola = nmpi.array(values)
lala = values.split()
print (split(hola,',')))

# for i in values(len(values)):
# 	print (i, end = " ")